import React from "react";

/**
 * === TEST ===
 * Create a text input for a "First name" and a submit button, when you click the submit button display the
 * name underneath the button (it doesn't need to look good, just work!)
 */
const TestComponent3: React.FC = () => {
  return null;
};

export { TestComponent3 };
