import React from "react";
import "./TestComponent2.css";

/**
 * === TEST ===
 * Create a blue box which is 300px in height and 300px in length, then create a red circle inside the blue box
 * which is 100px in diameter and center it.
 */
const TestComponent2: React.FC = () => {
  return null;
};

export { TestComponent2 };
